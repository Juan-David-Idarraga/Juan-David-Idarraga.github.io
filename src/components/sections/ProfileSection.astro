<section class="section profile" id="perfil">
  <div class="container profile-grid">
    <div data-reveal>
      <span class="eyebrow">Perfil profesional</span>
      <h2>Ingenieria en Ejecucion en Informatica con mentalidad de producto y operacion.</h2>
    </div>
    <div class="profile-copy" data-reveal>
      <p>
        Soy Juan David Idarraga, estudiante de Ingenieria en Ejecucion en Informatica. Mi foco esta en construir
        software que ayude a empresas a ordenar procesos reales: ventas, inventario, logistica, plataformas web,
        movilidad, reportes y automatizacion.
      </p>
      <p>
        Mi perfil combina ejecucion tecnica con criterio de negocio: entender el flujo, detectar restricciones,
        proponer arquitectura y construir una base limpia para iterar.
      </p>
    </div>
  </div>
</section>

<style>
  .profile {
    background:
      linear-gradient(90deg, rgba(212, 175, 55, 0.08), transparent 28%),
      rgba(5, 5, 5, 0.3);
  }

  .profile-grid {
    display: grid;
    grid-template-columns: minmax(0, 0.95fr) minmax(0, 1.05fr);
    gap: clamp(2rem, 5vw, 4rem);
  }

  h2 {
    margin: 1rem 0 0;
    font-family: var(--font-display);
    font-size: clamp(2rem, 4vw, 3.3rem);
    line-height: 1.08;
  }

  .profile-copy {
    display: grid;
    gap: 1rem;
  }

  p {
    margin: 0;
    color: var(--technical-silver);
    font-size: 1.04rem;
    line-height: 1.9;
  }

  @media (max-width: 820px) {
    .profile-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

